import React from 'react';

const Home = () => (
  <div className="info">
    <h1>Routing in React</h1>
    <p>
      This app implements routing using react router. It two nav links as home and teams.
      Teams components display a list of all Premier League Teams.
      On clicking on a team name, will take you to a page where you can
      view all its details.
    </p>
  </div>
);

export default Home;